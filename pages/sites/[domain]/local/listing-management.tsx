import { createLocalPage } from '../../../../components/local/createLocalPage';

export default createLocalPage({
  title: 'Listing Management',
  description: 'Manage and sync your business listings across directories. This tool is on its way.',
});
