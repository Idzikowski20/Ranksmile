import { createLocalPage } from '../../../../components/local/createLocalPage';

export default createLocalPage({
  title: 'Review Management',
  description: 'Track, respond to, and analyze customer reviews from Google and other platforms. This tool is on its way.',
});
