import { createLocalPage } from '../../../../components/local/createLocalPage';

export default createLocalPage({
  title: 'GBP AI Agent',
  description: 'Automate Google Business Profile updates and responses with AI. This automation is on its way.',
});
