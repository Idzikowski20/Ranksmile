import { createLocalPage } from '../../../../components/local/createLocalPage';

export default createLocalPage({
  title: 'GBP Optimization',
  description: 'Optimize your Google Business Profile with actionable recommendations. This tool is on its way.',
});
