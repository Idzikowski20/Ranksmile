import { createLocalPage } from '../../../../components/local/createLocalPage';

export default createLocalPage({
  title: 'Map Rank Tracker',
  description: 'Track local map pack rankings across locations and competitors. This tool is on its way.',
});
