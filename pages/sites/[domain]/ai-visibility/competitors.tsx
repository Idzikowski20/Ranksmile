import type { NextPage } from 'next';
import ComingSoon from '../../../../components/aiVisibility/ComingSoon';

const AiVisibilityCompetitors: NextPage = () => (
   <ComingSoon title="Competitors" blurb="Which brands the AI engines mention instead of you, and your share of voice against them across all tracked prompts." />
);

export default AiVisibilityCompetitors;
