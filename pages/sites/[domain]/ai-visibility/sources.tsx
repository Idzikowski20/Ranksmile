import type { NextPage } from 'next';
import ComingSoon from '../../../../components/aiVisibility/ComingSoon';

const AiVisibilitySources: NextPage = () => (
   <ComingSoon title="Sources" blurb="Every page each AI engine cited for your tracked prompts, with how often it was shown. Available after your first scan completes." />
);

export default AiVisibilitySources;
