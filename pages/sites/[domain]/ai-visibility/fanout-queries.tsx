import type { NextPage } from 'next';
import ComingSoon from '../../../../components/aiVisibility/ComingSoon';

const AiVisibilityFanoutQueries: NextPage = () => (
   <ComingSoon title="Fanout Queries" blurb="The follow-up searches AI engines run while answering your prompts — where you can win visibility next. Beta." />
);

export default AiVisibilityFanoutQueries;
